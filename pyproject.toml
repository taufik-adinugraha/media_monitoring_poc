[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "media-monitor"
version = "0.1.0"
description = "Metadata-first media monitoring: GDELT + MediaStack + RSS + YouTube, Gemini enrichment, Sonar deep analytics."
requires-python = ">=3.10"
